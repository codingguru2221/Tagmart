{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @workspace/tagmart",
    "dev:api": "npm run dev --workspace @workspace/api-server",
    "build": "npm run typecheck && npm --workspaces --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm --workspace @workspace/tagmart --workspace @workspace/api-server --workspace @workspace/mockup-sandbox --workspace @workspace/scripts --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
